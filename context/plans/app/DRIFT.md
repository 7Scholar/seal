# Drift — context/plans/app/

> Reconcile this by following `docs/plans/CODE_DRIFT.md` — read the docs and code, realign them, then re-record
> with the `add_to_coverage` / `remove_from_coverage` scripts. Never hand-edit `coverage.json` or this file: the
> stored value is the last-reconcile **commit SHA** (not a blob hash), and both files are script-generated.

## desktop/commands.md

- changed src-tauri/src/commands.rs
  1bd9a31 (12 files) "ui: the supervised password change, on a manifest that outlives the process"
- changed src-tauri/src/error.rs
  1bd9a31 (12 files) "ui: the supervised password change, on a manifest that outlives the process"

## desktop/shell.md

- changed src-tauri/src/lib.rs
  1bd9a31 (12 files) "ui: the supervised password change, on a manifest that outlives the process"

## desktop/ui/screens.md

- changed ui/App.tsx
  1bd9a31 (12 files) "ui: the supervised password change, on a manifest that outlives the process"
- changed ui/screens/RepoList.test.tsx
  1bd9a31 (12 files) "ui: the supervised password change, on a manifest that outlives the process"
- changed ui/screens/RepoList.tsx
  1bd9a31 (12 files) "ui: the supervised password change, on a manifest that outlives the process"
- changed ui/styles.css
  1bd9a31 (12 files) "ui: the supervised password change, on a manifest that outlives the process"

## desktop/ui/shell.md

- changed ui/ipc.ts
  1bd9a31 (12 files) "ui: the supervised password change, on a manifest that outlives the process"

## Uncovered

- src-tauri/src/rekey.rs
  1bd9a31 (born) "ui: the supervised password change, on a manifest that outlives the process"
- src-tauri/tests/rekey.rs
  1bd9a31 (born) "ui: the supervised password change, on a manifest that outlives the process"
- ui/screens/PasswordChange.test.tsx
  1bd9a31 (born) "ui: the supervised password change, on a manifest that outlives the process"
- ui/screens/PasswordChange.tsx
  1bd9a31 (born) "ui: the supervised password change, on a manifest that outlives the process"
