# Drift — context/plans/app/

> Reconcile this by following `docs/plans/CODE_DRIFT.md` — read the docs and code, realign them, then re-record
> with the `add_to_coverage` / `remove_from_coverage` scripts. Never hand-edit `coverage.json` or this file: the
> stored value is the last-reconcile **commit SHA** (not a blob hash), and both files are script-generated.

## desktop/first-open.md

- changed ui/App.tsx
  153f382 (20 files) "ui: the unhappy paths — plain-language failures, re-lock on expiry, native folder picker, the seal recency warning"
- changed ui/screens/Unlock.test.tsx
  153f382 (20 files) "ui: the unhappy paths — plain-language failures, re-lock on expiry, native folder picker, the seal recency warning"
- changed ui/screens/Unlock.tsx
  153f382 (20 files) "ui: the unhappy paths — plain-language failures, re-lock on expiry, native folder picker, the seal recency warning"

## desktop/shell.md

- changed src-tauri/Cargo.toml
  153f382 (20 files) "ui: the unhappy paths — plain-language failures, re-lock on expiry, native folder picker, the seal recency warning"
- changed src-tauri/capabilities/default.json
  153f382 (20 files) "ui: the unhappy paths — plain-language failures, re-lock on expiry, native folder picker, the seal recency warning"
- changed src-tauri/src/lib.rs
  153f382 (20 files) "ui: the unhappy paths — plain-language failures, re-lock on expiry, native folder picker, the seal recency warning"

## desktop/ui/password-change.md

- changed ui/screens/PasswordChange.test.tsx
  153f382 (20 files) "ui: the unhappy paths — plain-language failures, re-lock on expiry, native folder picker, the seal recency warning"
- changed ui/screens/PasswordChange.tsx
  153f382 (20 files) "ui: the unhappy paths — plain-language failures, re-lock on expiry, native folder picker, the seal recency warning"

## desktop/ui/screens.md

- changed ui/App.tsx
  153f382 (20 files) "ui: the unhappy paths — plain-language failures, re-lock on expiry, native folder picker, the seal recency warning"
- changed ui/screens/EnvEditor.test.tsx
  153f382 (20 files) "ui: the unhappy paths — plain-language failures, re-lock on expiry, native folder picker, the seal recency warning"
- changed ui/screens/EnvEditor.tsx
  153f382 (20 files) "ui: the unhappy paths — plain-language failures, re-lock on expiry, native folder picker, the seal recency warning"
- changed ui/screens/Unlock.test.tsx
  153f382 (20 files) "ui: the unhappy paths — plain-language failures, re-lock on expiry, native folder picker, the seal recency warning"
- changed ui/screens/Unlock.tsx
  153f382 (20 files) "ui: the unhappy paths — plain-language failures, re-lock on expiry, native folder picker, the seal recency warning"
- changed ui/styles.css
  153f382 (20 files) "ui: the unhappy paths — plain-language failures, re-lock on expiry, native folder picker, the seal recency warning"

## desktop/ui/shell.md

- changed package.json
  153f382 (20 files) "ui: the unhappy paths — plain-language failures, re-lock on expiry, native folder picker, the seal recency warning"

## publishing/docs.md

- changed README.md
  86ee907 (7 files) "journeys: close the holes a cold read found in the axis itself"

## Uncovered

- ui/components/Problem.test.tsx
  153f382 (born) "ui: the unhappy paths — plain-language failures, re-lock on expiry, native folder picker, the seal recency warning"
- ui/components/Problem.tsx
  153f382 (born) "ui: the unhappy paths — plain-language failures, re-lock on expiry, native folder picker, the seal recency warning"
- ui/errors.test.ts
  153f382 (born) "ui: the unhappy paths — plain-language failures, re-lock on expiry, native folder picker, the seal recency warning"
- ui/errors.ts
  153f382 (born) "ui: the unhappy paths — plain-language failures, re-lock on expiry, native folder picker, the seal recency warning"
