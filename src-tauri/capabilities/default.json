{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "The minimum the interface needs: its own window, Seal's own commands, and the folder-picking dialog.",
  "windows": ["main"],
  "permissions": ["core:default", "dialog:allow-open"]
}
